# Static inventory.
# Replace the ansible_host value with your instance's public IP, which you can
# read from Terraform:
#   terraform -chdir=../terraform output -raw instance_public_ip
#
# (A dynamic AWS inventory is also provided in inventory/aws_ec2.yml.)

[app]
smart-umuganda ansible_host=98.91.144.193

[app:vars]
ansible_user=ubuntu
