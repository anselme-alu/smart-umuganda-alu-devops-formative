# Local Terraform state and working directories
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Variable files may contain secrets — never commit real values.
# The example file is intentionally allowed.
*.tfvars
*.tfvars.json
!*.tfvars.example

# Backend configuration is environment-specific. The example file is allowed.
backend.hcl

# CLI config / plan artifacts
.terraformrc
terraform.rc
*.tfplan
tfplan

# SSH keys generated for the demo
*.pem
smart-umuganda-key*
